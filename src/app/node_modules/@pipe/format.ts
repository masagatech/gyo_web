import { NgModule } from '@angular/core';
import { format } from '../../_pipe/format';

@NgModule({
	declarations: [format],
	exports: [format],
})
export class FormatPipeModule { }

export * from '../../_pipe/format';